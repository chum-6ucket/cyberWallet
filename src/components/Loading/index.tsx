import {FC} from "react";
import {Container} from "./styles";

const Loading: FC = (): JSX.Element => (
    <Container>
        <img src="" alt="AirWallet" aria-label="AirWallet" title="AirWallet"/>
    </Container>
);

export default Loading;
